#!/bin/sh
# Run Spotless (remove unused imports, format) on staged Java files
if git diff --cached --name-only | grep -q '\.java$'; then
  (cd Backend && ./gradlew spotlessApply)
  git add Backend/
fi
